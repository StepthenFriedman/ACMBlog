#include <stdio.h>
#include <limits.h>

char ipt[1000000];
int main(){
    int l,i,count;
    scanf("%d%s",&l,ipt);
    if (l>3){
        int repeat=INT_MAX;
        count=0;
        for (i=0;i<l;i++){
            if (i)
        }
    }
}